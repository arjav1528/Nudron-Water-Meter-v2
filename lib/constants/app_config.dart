import 'package:flutter/material.dart';

class ConfigurationCustom {
  static bool isTest = false;
  // static Widget testScreen = Scaffold(
  //     body: BillingFormulaDialog(billingFormula: null, parentContext: null,));
static Widget testScreen = Container();
  //TODO: CHECK BEFORE BUILD
  static bool skipAnyAuths = false;
  static bool isLargerTextField = false;
}
